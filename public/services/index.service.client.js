(function () {
    angular
        .module("GoHiking")
        .factory("IndexService", IndexService);

    function IndexService($http) {
        var services = {

        };
        return services;

    }
})();